# Copy to frontend/.env (npm run setup:env does this).
# Used by Vite for local `npm run -w frontend dev`.

VITE_API_URL=http://localhost:4000/api
# Optional public backend origin for webhook/widget copy-paste helpers
VITE_PUBLIC_WEBHOOK_BASE_URL=http://localhost:4000
